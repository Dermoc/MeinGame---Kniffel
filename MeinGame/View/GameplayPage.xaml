<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    x:Class="MeinGame.View.GameplayPage"
    xmlns="http://schemas.microsoft.com/dotnet/maui/global"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:d="http://schemas.microsoft.com/dotnet/maui/global/design"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodel="clr-namespace:MeinGame.ViewModel"
    xmlns:local="clr-namespace:MeinGame.View"
    mc:Ignorable="d">
    <ContentPage.Content>




        <Grid ColumnDefinitions="*, Auto">


            <Grid Grid.Column="0">

                <Grid ColumnDefinitions="*,*,*"
                      RowDefinitions="Auto,*,*,*,*,*,*,*,*,*,*,*,*,*,*,*,*,*,*">

                    <!-- Tabellenkopf (Zeile 0) -->
                    <Border Grid.Row="0" Grid.Column="0">
                        <Label Text="Kategorie" FontAttributes="Bold"/>
                    </Border>
                    <Border Grid.Row="0" Grid.Column="1">
                        <Label Text="{Binding Spieler1.PlayerName}" FontAttributes="Bold"/>
                    </Border>
                    <Border Grid.Row="0" Grid.Column="2">
                        <Label Text="{Binding Spieler2.PlayerName}" FontAttributes="Bold"/>
                    </Border>

                    <!-- EINSER (Zeile 1) -->
                    <Border Grid.Row="1" Grid.Column="0">
                        <Label Text="1"/>
                    </Border>
                    <Border Grid.Row="1" Grid.Column="1">
                        <Label Text="{Binding Spieler1.Einser}"/>
                    </Border>
                    <Border Grid.Row="1" Grid.Column="2">
                        <Label Text="{Binding Spieler2.Einser}"/>
                    </Border>

                    <!-- ZWEIER (Zeile 2) -->
                    <Border Grid.Row="2" Grid.Column="0">
                        <Label Text="2"/>
                    </Border>
                    <Border Grid.Row="2" Grid.Column="1">
                        <Label Text="{Binding Spieler1.Zweier}"/>
                    </Border>
                    <Border Grid.Row="2" Grid.Column="2">
                        <Label Text="{Binding Spieler2.Zweier}"/>
                    </Border>

                    <!-- DREIER (Zeile 3) -->
                    <Border Grid.Row="3" Grid.Column="0">
                        <Label Text="3"/>
                    </Border>
                    <Border Grid.Row="3" Grid.Column="1">
                        <Label Text="{Binding Spieler1.Dreier}"/>
                    </Border>
                    <Border Grid.Row="3" Grid.Column="2">
                        <Label Text="{Binding Spieler2.Dreier}"/>
                    </Border>

                    <!-- VIERER (Zeile 4) -->
                    <Border Grid.Row="4" Grid.Column="0">
                        <Label Text="4"/>
                    </Border>
                    <Border Grid.Row="4" Grid.Column="1">
                        <Label Text="{Binding Spieler1.Vierer}"/>
                    </Border>
                    <Border Grid.Row="4" Grid.Column="2">
                        <Label Text="{Binding Spieler2.Vierer}"/>
                    </Border>

                    <!-- FÜNFER (Zeile 5) -->
                    <Border Grid.Row="5" Grid.Column="0">
                        <Label Text="5"/>
                    </Border>
                    <Border Grid.Row="5" Grid.Column="1">
                        <Label Text="{Binding Spieler1.Fuenfer}"/>
                    </Border>
                    <Border Grid.Row="5" Grid.Column="2">
                        <Label Text="{Binding Spieler2.Fuenfer}"/>
                    </Border>

                    <!-- SECHSER (Zeile 6) -->
                    <Border Grid.Row="6" Grid.Column="0">
                        <Label Text="6"/>
                    </Border>
                    <Border Grid.Row="6" Grid.Column="1">
                        <Label Text="{Binding Spieler1.Sechser}"/>
                    </Border>
                    <Border Grid.Row="6" Grid.Column="2">
                        <Label Text="{Binding Spieler2.Sechser}"/>
                    </Border>

                    <!-- GESAMT OBEN (Zeile 7) -->
                    <Border Grid.Row="7" Grid.Column="0">
                        <Label Text="Gesamt Oben" FontAttributes="Bold"/>
                    </Border>
                    <Border Grid.Row="7" Grid.Column="1">
                        <Label Text="{Binding Spieler1.GesamtOben}" FontAttributes="Bold"/>
                    </Border>
                    <Border Grid.Row="7" Grid.Column="2">
                        <Label Text="{Binding Spieler2.GesamtOben}" FontAttributes="Bold"/>
                    </Border>

                    <!-- BONUS (Zeile 8) -->
                    <Border Grid.Row="8" Grid.Column="0">
                        <Label Text="Bonus"/>
                    </Border>
                    <Border Grid.Row="8" Grid.Column="1">
                        <Label Text="{Binding Spieler1.Bonus}"/>
                    </Border>
                    <Border Grid.Row="8" Grid.Column="2">
                        <Label Text="{Binding Spieler2.Bonus}"/>
                    </Border>

                    <!-- ÜBERTRAG OBEN (Zeile 9) -->
                    <Border Grid.Row="9" Grid.Column="0">
                        <Label Text="Übertrag Oben" FontAttributes="Bold"/>
                    </Border>
                    <Border Grid.Row="9" Grid.Column="1">
                        <Label Text="{Binding Spieler1.GesamtOben}" FontAttributes="Bold"/>
                    </Border>
                    <Border Grid.Row="9" Grid.Column="2">
                        <Label Text="{Binding Spieler2.GesamtOben}" FontAttributes="Bold"/>
                    </Border>

                    <!-- 3ER PASCH (Zeile 10) -->
                    <Border Grid.Row="10" Grid.Column="0">
                        <Label Text="3er Pasch"/>
                    </Border>
                    <Border Grid.Row="10" Grid.Column="1">
                        <Label Text="{Binding Spieler1.DreierPasch}"/>
                    </Border>
                    <Border Grid.Row="10" Grid.Column="2">
                        <Label Text="{Binding Spieler2.DreierPasch}"/>
                    </Border>

                    <!-- 4ER PASCH (Zeile 11) -->
                    <Border Grid.Row="11" Grid.Column="0">
                        <Label Text="4er Pasch"/>
                    </Border>
                    <Border Grid.Row="11" Grid.Column="1">
                        <Label Text="{Binding Spieler1.ViererPasch}"/>
                    </Border>
                    <Border Grid.Row="11" Grid.Column="2">
                        <Label Text="{Binding Spieler2.ViererPasch}"/>
                    </Border>

                    <!-- FULL HOUSE (Zeile 12) -->
                    <Border Grid.Row="12" Grid.Column="0">
                        <Label Text="Full House"/>
                    </Border>
                    <Border Grid.Row="12" Grid.Column="1">
                        <Label Text="{Binding Spieler1.FullHouse}"/>
                    </Border>
                    <Border Grid.Row="12" Grid.Column="2">
                        <Label Text="{Binding Spieler2.FullHouse}"/>
                    </Border>

                    <!-- KL. STRASSE (Zeile 13) -->
                    <Border Grid.Row="13" Grid.Column="0">
                        <Label Text="Kl. Strasse"/>
                    </Border>
                    <Border Grid.Row="13" Grid.Column="1">
                        <Label Text="{Binding Spieler1.KleineStrasse}"/>
                    </Border>
                    <Border Grid.Row="13" Grid.Column="2">
                        <Label Text="{Binding Spieler2.KleineStrasse}"/>
                    </Border>

                    <!-- GR. STRASSE (Zeile 14) -->
                    <Border Grid.Row="14" Grid.Column="0">
                        <Label Text="Gr. Strasse"/>
                    </Border>
                    <Border Grid.Row="14" Grid.Column="1">
                        <Label Text="{Binding Spieler1.GrosseStrasse}"/>
                    </Border>
                    <Border Grid.Row="14" Grid.Column="2">
                        <Label Text="{Binding Spieler2.GrosseStrasse}"/>
                    </Border>

                    <!-- JEPJEP (Zeile 15) -->
                    <Border Grid.Row="15" Grid.Column="0">
                        <Label Text="JepJep"/>
                    </Border>
                    <Border Grid.Row="15" Grid.Column="1">
                        <Label Text="{Binding Spieler1.JepJep}"/>
                    </Border>
                    <Border Grid.Row="15" Grid.Column="2">
                        <Label Text="{Binding Spieler2.JepJep}"/>
                    </Border>

                    <!-- CHANCE (Zeile 16) -->
                    <Border Grid.Row="16" Grid.Column="0">
                        <Label Text="Chance"/>
                    </Border>
                    <Border Grid.Row="16" Grid.Column="1">
                        <Label Text="{Binding Spieler1.Chance}"/>
                    </Border>
                    <Border Grid.Row="16" Grid.Column="2">
                        <Label Text="{Binding Spieler2.Chance}"/>
                    </Border>

                    <!-- GESAMT UNTEN (Zeile 17) -->
                    <Border Grid.Row="17" Grid.Column="0">
                        <Label Text="Gesamt Unten" FontAttributes="Bold"/>
                    </Border>
                    <Border Grid.Row="17" Grid.Column="1">
                        <Label Text="{Binding Spieler1.GesamtUnten}" FontAttributes="Bold"/>
                    </Border>
                    <Border Grid.Row="17" Grid.Column="2">
                        <Label Text="{Binding Spieler2.GesamtUnten}" FontAttributes="Bold"/>
                    </Border>

                    <!-- GESAMT ALLES (Zeile 18) -->
                    <Border Grid.Row="18" Grid.Column="0">
                        <Label Text="Gesamt Alles" FontAttributes="Bold"/>
                    </Border>
                    <Border Grid.Row="18" Grid.Column="1">
                        <Label Text="{Binding Spieler1.Endsumme}" FontAttributes="Bold"/>
                    </Border>
                    <Border Grid.Row="18" Grid.Column="2">
                        <Label Text="{Binding Spieler2.Endsumme}" FontAttributes="Bold"/>
                    </Border>

                </Grid>
            </Grid>



            <Grid Grid.Column="1"
                  RowDefinitions="*,*">

                <!-- RECHTE SEITE: Würfel und Button -->
                <Grid Grid.Column="1" RowDefinitions="Auto, *, Auto" Margin="10">

                    <!-- 1. Überschrift -->
                    <Label Grid.Row="0"
                       Text="Würfel und Ergebnisse"
                       FontAttributes="Bold"
                       HorizontalOptions="Center"
                       Margin="0,0,0,20" />

                    <!-- 2. Die 5 Würfel (werden aus der WuerfelListe generiert) -->
                    <HorizontalStackLayout Grid.Row="1"
                                       BindableLayout.ItemsSource="{Binding WuerfelListe}"
                                       Spacing="10"
                                       HorizontalOptions="Center"
                                       VerticalOptions="Center">

                        <BindableLayout.ItemTemplate>
                            <DataTemplate>
                                <!-- Das Design für EINEN einzelnen Würfel -->
                                <Border WidthRequest="60" 
                                    HeightRequest="60" 
                                    Stroke="Black" 
                                    StrokeThickness="2"
                                    StrokeShape="RoundRectangle 8">

                                    <!-- Klick-Erkennung für das Sperren -->
                                    <Border.GestureRecognizers>
                                        <TapGestureRecognizer 
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodel:GameplayViewModel}}, Path=WuerfelSperrenCommand}"
                                        CommandParameter="{Binding .}" />
                                    </Border.GestureRecognizers>

                                    <!-- Farbwechsel, wenn der Würfel gesperrt wird -->
                                    <Border.Triggers>
                                        <DataTrigger TargetType="Border" Binding="{Binding IstGesperrt}" Value="True">
                                            <Setter Property="BackgroundColor" Value="LightGray" />
                                        </DataTrigger>
                                        <DataTrigger TargetType="Border" Binding="{Binding IstGesperrt}" Value="False">
                                            <Setter Property="BackgroundColor" Value="White" />
                                        </DataTrigger>
                                    </Border.Triggers>

                                    <!-- Anzeige der Würfel-Zahl -->
                                    <Label Text="{Binding Wert}" 
                                       FontSize="28" 
                                       FontAttributes="Bold" 
                                       TextColor="Black"
                                       HorizontalOptions="Center" 
                                       VerticalOptions="Center" />
                                </Border>
                            </DataTemplate>
                        </BindableLayout.ItemTemplate>
                    </HorizontalStackLayout>

                    <!-- 3. Der Würfeln-Button (MVVM konform) -->
                    <Button Grid.Row="2"
                        Text="Würfeln"
                        Command="{Binding WuerfelnCommand}"
                        Background="BlanchedAlmond"
                        TextColor="Black"
                        FontAttributes="Bold"
                        HeightRequest="50" />
                </Grid>
            </Grid>











        </Grid>
    </ContentPage.Content>
</ContentPage>